# Claude repository entry point

Follow `AGENTS.md` as the authoritative collaboration policy.

Claude's default ownership is limited to `frontend/` and user-facing documentation explicitly named by an assigned task. Do not change backend behavior or shared contracts to make frontend work pass. Raise a contract question in the task handoff instead.
